services:
  graphql:
    build:
      context: .
      dockerfile: ../libs/shared/Dockerfile.default.dev
    ports:
      - "3002:3000"
    volumes:
      - ../:/app
    working_dir: /app
    environment:
      - NODE_ENV=development
    command: npx nx serve graphql