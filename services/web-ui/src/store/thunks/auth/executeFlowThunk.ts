import { socket } from '@web-ui/socket/socket';

export const executeFlowThunk = (nodes: any, edges: any, token: string) => async (dispatch: any) => {
  console.log('=== executeFlowThunk ===');
  
  // socket.to('').emit('')
};
